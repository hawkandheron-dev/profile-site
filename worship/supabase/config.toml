# Supabase project for the worship scheduler (separate from the windhover
# project configured in /supabase). project_id is filled in once the
# project is created — see docs/worship-scheduler.md.
project_id = "WORSHIP_PROJECT_REF"

[api]
port = 54331
